.user_area
  #user_set
    %p.user_name
      = message.user.name
    %p.date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  #user_set
    - if message.content.present?
      %p.user_message_up
        = message.content
    = image_tag message.image.url, class: 'user_set__image' if message.image.present?
